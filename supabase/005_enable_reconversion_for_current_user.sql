-- Este portal no provisiona aplicaciones por defecto.
-- Mantener este archivo como no-op evita reintroducir el catalogo heredado.

select
  'Sin aplicaciones precargadas. Crea y asigna aplicaciones manualmente cuando el portal las necesite.' as info;
